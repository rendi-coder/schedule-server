import { createGroup } from './create-group';
import { getGroups, getGroup } from './get-groups';

const groupController = {
  createGroup,
  getGroup,
  getGroups,
};

export default groupController;
