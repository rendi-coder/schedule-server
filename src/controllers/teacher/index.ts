import { createTeacher } from './create-teacher';
import { getTeachers, getTeacher } from './get-teachers';

const teachersController = {
  createTeacher,
  getTeacher,
  getTeachers,
};

export default teachersController;
